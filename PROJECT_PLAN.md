# 승정원일기 번역 서빙 시스템 — 프로젝트 계획서

> 선행 연구: [dainkil/sjw_traslator](https://github.com/dainkil/sjw_traslator) — 지식 주입형 언어 모델 기반 승정원일기 번역 모델 연구
> 본 프로젝트: 위 연구 파이프라인을 **운영 가능한 번역 시스템**으로 전환하고, 서버/백엔드 직무 포트폴리오로 사용한다.

---

## 0. 이 문서의 사용법 (Claude Code 지침)

이 문서는 에이전트가 작업을 집행하기 위한 계약서다. 다음 규칙을 지킨다.

1. **작업 시작 전 반드시 `§7 명시적 배제 목록`과 `§11 ADR 목록`을 먼저 읽는다.** 배제 목록에 있는 기술을 도입하려면 먼저 사람에게 확인받는다.
2. **마일스톤 단위로 진행한다.** `§10`의 수용 기준(Acceptance Criteria)을 만족하지 못한 마일스톤은 완료로 표시하지 않는다.
3. **새로운 의존성 추가 = ADR 작성.** `docs/adr/NNN-*.md`에 배경 / 선택지 / 결정 / 버린 대안과 이유를 기록하지 않은 의존성은 추가하지 않는다.
4. **모든 성능·비용 주장은 실측 기반이어야 한다.** 추정치는 반드시 `(추정)` 표기와 가정을 명시한다.
5. **"AI 프로젝트니까 넣는다"는 금지.** 이 프로젝트의 평가 기준은 기능 개수가 아니라 결정의 방어 가능성이다.

---

## 1. 프로젝트 정의

### 1.1 선행 연구 자산 (재사용 대상)

| 자산 | 내용 | 본 프로젝트에서의 역할 |
|---|---|---|
| SillokBERT CPT 모델 | 인조 연간 승정원일기 MLM 계속사전학습 | NER 백본 |
| SillokBERT-NER | 인물/관직/지명/날짜 BIO 9태그 | **비용 라우터 겸 캐시 슬롯 추출기** (재해석) |
| `id_lookup_injo.json` | 2,690명 인물 메타데이터 | KB 원천 레코드 |
| `inverted_index_injo.json` | 9,403 키 역색인 | 결정론적 엔티티 링킹 |
| 3단계 Fallback 링킹 로직 | 표층형 → 글자 교집합 → 글자 합집합 | 그대로 이식 |
| Gemini 2.5 Flash 프롬프트 | 원문 + 엔티티 맥락 + 번역관 페르소나 | PromptTemplate으로 구조화 |

### 1.2 프레이밍 — 이 프로젝트가 주장하는 것

**금지 프레이밍:** "승정원일기 번역 API를 만들고 배포했습니다."
소재만 특이하고 구조는 흔하다. 변별력 없음.

**채택 프레이밍:**

> **2062년으로 추산된 승정원일기 완역 시점을, 얼마의 비용으로 언제까지 당길 수 있는가.**

근거: 국역 완료율 37.4%, 잔여 약 1억 5천만 자, 현행 속도 유지 시 완역 2062년 추산 (선행 연구 README).

이 프레이밍이 주는 것:
- **헤드라인 숫자**가 생긴다 (면접관이 5초 안에 이해하는 것)
- 처리량·비용이 목적 함수가 되므로 **모든 인프라 결정에 자동으로 당위성이 붙는다**
- 동일 문제 정의를 가진 포트폴리오가 사실상 없다

### 1.3 목표 / 비목표

**목표**
- 번역 요청을 받아 NER → KB 링킹 → 프롬프트 증강 → LLM 번역까지 수행하는 운영 가능한 서비스
- 대규모 배치 번역 엔진 (중단·재개·멱등·예산 통제)
- **잔여 코퍼스 완역의 비용/시간 모델**을 실측 기반으로 산출
- 모든 아키텍처 결정에 대한 ADR 문서

**비목표 (명시적으로 하지 않음)**
- 번역 품질 자체의 추가 개선 (모델 재학습, 파인튜닝)
- 전체 코퍼스 실제 완역 (유계 구간만 실행 후 외삽)
- 프론트엔드 완성도
- BLEU 등 번역 품질 지표 경쟁

---

## 2. 핵심 제약 — 모든 결정의 근거

### 2.1 지연 예산 분해

| 단계 | 병목 성격 | 지연 (M1 실측, 2026-08-31) | 통제 가능 |
|---|---|---|---|
| NER 추론 (ONNX INT8, CPU) | CPU, 모델 상주 178MB | **8.3ms** (p50, 서버 내) / 19~24ms (HTTP 왕복 포함) | O |
| KB 역색인 조회 | 메모리, 9,403 키 | **≤ 1ms** | O |
| 프롬프트 조립 | CPU | **≤ 1ms** | O |
| LLM 호출 (무료 티어) | 네트워크, quota, 모델별 상이 | **p50 3.0s / p95 6.5s** (3.1-flash-lite) · **p50 12.0s / p95 18.8s** (3.5-flash, thinking) | **X** |

> **핵심 사실 (실측 확정): 전체 p95의 99.7~99.9%가 통제 밖 외부 API에 지배된다.**
> 계획 시 가설("90% 이상")을 상회 확정. 이 문장이 §3의 모든 결정의 유일한 근거다. 상세: `docs/benchmarks.md`.

### 2.2 여기서 도출되는 원칙

1. **동기 요청/응답은 부적합하다.** 스레드가 남의 API 지연에 묶인다.
2. **내부 최적화(GPU 등)의 ROI가 낮다.** NER을 300ms → 30ms로 줄여도 체감 개선 1~2%.
3. **가장 큰 레버는 "호출을 안 하는 것"이다.** → 캐싱과 라우팅이 최우선 최적화 대상.
4. **1차 운영 지표는 CPU가 아니라 돈이다.**

---

## 3. 아키텍처

### 3.1 전체 구조

```
                    [클라이언트 / 데모 UI]
                             │
                             ▼
        ┌────────────────────────────────────────┐
        │  API 서버 (Spring Boot)                 │
        │  · 인증 / API 키 발급                    │
        │  · rate limit (사용자별)                 │
        │  · 요청 검증 → job 발행 → 202 반환        │
        │  · 결과 조회 / SSE 스트리밍               │
        └────────────────────────────────────────┘
                             │ 발행
                             ▼
              [Redis Streams — 작업 큐 + DLQ]
                             │ 소비
                             ▼
        ┌────────────────────────────────────────┐
        │  Worker (Spring Boot)                   │
        │                                         │
        │  ① 캐시 조회 (원문해시 → 템플릿해시)      │
        │  ② NER 호출 ──────────► [Python NER 서버] │
        │  ③ KB 링킹 (in-memory 역색인)            │
        │  ④ 티어 라우팅 결정                       │
        │  ⑤ 프롬프트 조립  ← Spring AI            │
        │  ⑥ LLM 호출     ← Spring AI + Resilience4j│
        │  ⑦ 결과 저장 + 캐시 적재 + 비용 기록       │
        └────────────────────────────────────────┘
                    │                    │
                    ▼                    ▼
            [PostgreSQL]            [Redis]
         결과 / job 이력 /        job 상태 / 번역 캐시 /
         배치 체크포인트 /         적응형 rate 토큰버킷 /
         비용 원장                예산 카운터
                    │
                    ▼
      [Prometheus + Grafana] ← Actuator/Micrometer
```

### 3.2 컴포넌트별 책임

| 컴포넌트 | 런타임 | 책임 | 스케일 축 |
|---|---|---|---|
| API 서버 | Spring Boot (JVM) | 수용, 검증, 인증, 조회 | 요청 수 |
| Worker | Spring Boot (JVM) | 파이프라인 실행, LLM 호출 | **외부 API 동시성 상한** |
| NER 서버 | Python + ONNX Runtime | 토큰 분류 추론 전용 | CPU / 메모리 |
| PostgreSQL | — | 결과, 이력, 체크포인트, 비용 원장 | — |
| Redis | — | 큐, 캐시, rate 상태, 예산 카운터 | — |

### 3.3 결정과 당위성

| # | 결정 | 당위성 | 이 결정이 없었다면 |
|---|---|---|---|
| D1 | **비동기 큐 기반 처리** | 외부 API가 1~3초. 동기 시 API 동시성이 남의 지연에 종속. 배치(수천 문장)는 동기로 불가능 | API 스레드풀 고갈, 배치 유스케이스 자체가 성립 안 함 |
| D2 | **Python NER 서버 분리** | 런타임 상이, 메모리 프로파일 상이(모델 상주 ~1GB), 스케일 축 상이. 합치면 API 인스턴스마다 모델이 따라 올라감 | 스케일아웃 비용 급증. **또한 이 분리가 Spring을 쓰는 당위성 자체** |
| D3 | **GPU 배제, ONNX INT8 CPU 서빙** | 전체 지연의 90%가 외부 API. NER 최적화 ROI가 1~2%. 측정 후 의도적 배제 | 근거 없는 GPU 인스턴스 비용. "측정하고 안 했다"는 서사를 잃음 |
| D4 | **KB in-memory 유지 (Redis 외부화 안 함)** | 2,690 레코드 + 9,403 키 = 수 MB. 워커 메모리에 상주가 적정 | 근거 없는 외부화 = 오버엔지니어링. 면접에서 찔림 |
| D5 | **Redis는 큐·캐시·rate·예산 용도로만** | 이 용도들은 워커 간 공유 상태가 실제로 필요함 | 워커 무상태화 실패, 오토스케일 불가 |
| D6 | **Resilience4j 서킷브레이커 + 재시도** | 외부 LLM은 429/503가 일상. 재시도 없으면 실패율 직결, 무한 재시도는 quota 소진 | 정상 운영 불가 |
| D7 | **단건은 SSE 스트리밍 병행** | 데모 UX상 3초 대기는 사용자에게 정지로 보임. Spring AI `stream()` 활용 | 데모가 죽어 보임 |

---

## 4. 기술 스택

### 4.1 확정 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| API / Worker | Spring Boot + Java 21 | |
| LLM 연동 | **Spring AI** | 버전은 §4.2 참조 |
| LLM | Gemini 2.5 Flash (기본 티어) | 선행 연구와 동일, temperature=0.2 |
| 내결함성 | Resilience4j | 서킷브레이커 / 재시도 / 벌크헤드 |
| 큐 | Redis Streams | Consumer Group + DLQ. **Kafka 배제 — §7** |
| DB | PostgreSQL | |
| NER 서빙 | Python + FastAPI + ONNX Runtime (INT8) | |
| 관측성 | Micrometer → Prometheus → Grafana | |
| CI/CD | GitHub Actions → GHCR → 배포 | |
| 부하 테스트 | k6 | 외부 API mock 상태에서 내부 처리량 측정 |

### 4.2 Spring AI 버전 결정

**사실관계 (2026-08 기준):**
- Spring AI **2.0.0 GA** 릴리스: 2026-06-12. Spring Boot 4.0/4.1 + Spring Framework 7.0 대상. Jackson 3 마이그레이션 포함.
- Spring AI **1.1.x** 계열: Spring Boot 3.x / Framework 6 대상. 유지보수 중.

**결정: 2.0.0 GA 채택.** 신규 프로젝트라 레거시 제약이 없고, 포폴 시점 기준 최신 스택이 유리하다.
**리스크:** GA 후 3개월 미만이라 예제·생태계 성숙도가 낮다. 진행 중 블로커 발생 시 1.1.x로 폴백하고 그 사실을 ADR-005에 기록한다.

**Gemini 연동:**
- 스타터: `spring-ai-starter-model-google-genai`
- 인증 모드 2종: Gemini Developer API (API 키) / Vertex AI (GCP 자격증명)
- 프로퍼티 접두사: `spring.ai.google.genai`

### 4.3 알려진 함정

> **`project-id` 또는 `location` 프로퍼티를 설정하면 클라이언트가 Vertex AI 모드로 전환되어, Developer API 키가 400 인증 오류로 거부된다.**
> Developer API를 쓸 경우 `api-key`만 설정하고 `project-id` / `location`은 완전히 제거한다. 이 함정은 `README` 트러블슈팅 섹션에 기록한다.

추가 확인 필요 항목 (M1에서 검증):
- `limit: 0` 형태의 quota 오류는 실제 quota 문제가 아니라 해당 모델의 무료 티어 미제공을 의미할 수 있음
- Spring AI 2.0의 Jackson 3 전환이 기존 JSON 처리 코드와 충돌하는지

### 4.4 Spring AI — 쓸 것 / 안 쓸 것

**쓸 것**

| 기능 | 용도 | 당위성 |
|---|---|---|
| `PromptTemplate` | "원문 + 엔티티 맥락 + 번역관 페르소나" 조립 | 선행 연구 5단계가 정확히 템플릿 작업. 프롬프트 버전 관리와 A/B가 코드 수정 없이 가능해짐 |
| Structured Output (`BeanOutputConverter`) | 번역문 + 사용 엔티티 목록 + 불확실 구간을 타입 안전 객체로 수신 | 선행 연구는 결과 텍스트에서 한자 병기 횟수를 세는 정규식 후처리. 스키마로 보장받아야 함 |
| `ChatClient` 추상화 | provider 교체 | **핵심 당위성 — 아래 별도 설명** |
| Micrometer 연동 | 토큰 사용량 / 호출 지연 자동 계측 | 비용 SLI(§5.3)의 데이터 소스. 직접 HTTP 호출 시 전부 수작업 |
| `stream()` | 단건 SSE | 데모 체감 지연 개선 |

> **`ChatClient` 추상화의 당위성 (가장 중요):**
> 선행 연구 README의 한계 항목에 *"Gemini 2.5 Flash 자체의 번역 품질이 높아 KB 연계의 차별적 기여가 인명 경계 모호·동음이의 사례에 집중된다"* 고 기록되어 있다.
> 즉 **더 약한 모델로 교체하여 KB 주입 효과를 크게 만드는 것이 연구의 자연스러운 후속**이며, provider 교체가 실험 설계의 일부가 된다.
> "추상화를 선호해서"가 아니라 "내 연구의 명시된 한계가 provider 교체를 요구해서"라는 근거는 반박이 어렵다. 이 논리를 README와 ADR-005에 명시한다.

**안 쓸 것 (§7 참조)** — VectorStore/RAG 어드바이저, ChatMemory, Tool Calling

---

## 5. 차별화 축 (핵심 3개)

세 축은 하나의 논지로 수렴한다:
> **외부 LLM 비용이 지배하는 시스템에서, 비용을 일급 운영 지표로 다루고 도메인 지식으로 이를 깎았다.**

### 5.1 NER을 번역 보조가 아니라 **비용 라우터**로 재해석

선행 연구에서 NER은 품질 향상 도구였다. 서버 관점에서는 **LLM 호출 이전에 이미 실행되어 있는, 추가 비용이 0인 난이도 분류기**다.

**라우팅 규칙**

| 조건 | 티어 | 처리 |
|---|---|---|
| 엔티티 0개 + 정형문 패턴 매칭 | T0 | 캐시 우선, 미스 시 최저가 모델 |
| 엔티티 1~2개 + KB 전건 매칭 | T1 | 기본 모델 (Gemini 2.5 Flash) |
| KB 미매칭 존재 or 동명이인 모호 플래그 | T2 | 고성능 모델 + 결과 재검증 |

**신호 추출 항목:** 엔티티 개수, 태그 분포(PER/POS/LOC/DAT), KB 매칭 실패 건수, 3단계 Fallback 중 몇 단계에서 매칭됐는지, 활동시기·관직 교차검증 통과 여부

**측정 산출물:** 라우팅 적용 전후의 **비용/품질 곡선**. "품질 동등 유지하며 비용 N% 절감"이 목표 서술.

**주의:** 품질 평가는 선행 연구의 `05_eval_assets` 골든셋을 재사용한다. 새 평가 체계를 만들지 않는다.

### 5.2 도메인 구조를 반영한 **템플릿 슬롯 캐싱**

승정원일기는 의례적 정형문 비중이 매우 높다 (`上曰`, `傳曰`, 관직 임명 형식문 등). 단순 원문 해시 캐싱보다 훨씬 큰 히트율을 얻을 수 있다.

**동작**

```
원문 ──► NER로 인명/날짜/관직 추출
     ──► 슬롯 치환:  "命{PER}爲{POS}"  →  template_hash
     ──► 캐시 조회
          HIT  → 번역 템플릿에 엔티티 재주입 → 반환 (LLM 호출 0회)
          MISS → 전체 파이프라인 실행 → 템플릿화하여 캐시 적재
```

**2단 캐시 구조**
1. L1: 원문 정규화 해시 (완전 일치)
2. L2: 템플릿 슬롯 해시 (구조 일치)

**이것이 참신한 이유:** 도메인을 모르면 설계 불가능한 최적화다. "Redis 캐시를 붙였습니다"와 층위가 다르다. 그리고 여기서 NER이 두 번째로 재사용된다.

**반드시 다뤄야 할 난제 (문서화 대상):**
- **KB 갱신 시 무효화**: KB의 인물 레코드가 수정되면 어떤 캐시가 무효화되어야 하는가?
  - 선택지 A: `kb_version`을 캐시 키에 포함 (단순, 전면 무효화)
  - 선택지 B: 엔티티 ID → 캐시 키 역참조 인덱스 유지 (정밀, 복잡)
  - 결정과 근거를 ADR-009에 기록한다. **이 고민의 문서화 자체가 실력의 증거다.**
- 템플릿 재주입 시 어순·조사 처리 실패 케이스 (한국어 번역문에 엔티티 재삽입 시)
- 재주입 결과의 검증 방법 (샘플링 후 원본 파이프라인 결과와 대조)

### 5.3 비용을 SLI로, **예산을 서킷브레이커**로

장애율 기반 서킷브레이커는 흔하다. **예산 소진 기반 차단은 거의 없다.**

**대시보드 필수 패널**
- 문장당 비용 (원)
- 캐시 절감액 (L1/L2 분리)
- 티어별 요청 분포 및 티어별 단가
- 일일 예산 소진율 및 소진 예상 시각
- 토큰/요청 (입력/출력 분리)
- 429 발생률 및 적응형 rate 현재값

**단계적 성능 저하 (Graceful Degradation)**

| 예산 소진율 | 동작 |
|---|---|
| 70% | 경고 알림, T2 라우팅 중단 (T1로 하향) |
| 85% | 캐시 히트만 즉시 응답, 미스는 큐 적재 후 지연 처리 |
| 100% | 신규 job 수용 거부 (429 + 재개 예정 시각 반환) |

**배치 예산 사전 검증:** 배치 작업 생성 시 `budget_limit` 파라미터 필수. 잔여 예산 대비 예상 비용이 초과하면 **시작 전에 거부**한다.

---

## 6. 기본기 (없으면 위 3개도 신뢰받지 못함)

| 항목 | 요구사항 | 실패 시 인상 |
|---|---|---|
| **적응형 rate control** | 429 응답을 피드백으로 토큰 버킷 용량 자가 조정 (AIMD 등). 고정 상수 금지 | "실제로 안 돌려봤구나" |
| **체크포인트 / 재개** | 배치는 반드시 중간에 죽는다. 마지막 커서부터 재개 가능해야 함 | 대규모 처리 경험 없음 |
| **멱등 키** | 재실행 시 중복 LLM 호출 = 중복 과금이 없어야 함 | 과금 시스템 감각 없음 |
| **DLQ + 실패 분류** | 429 / 5xx / 타임아웃 / 콘텐츠 필터 / 파싱 실패는 각각 다른 처리 경로 | 에러 핸들링 미숙 |
| **비결정 출력 CI** | 골든셋 + 통계적 임계값. 프롬프트 수정 시 회귀 자동 감지 | LLM 시스템 운영 이해 부족 |
| **API 키 + 사용량 제한** | 공개 배포 시 타인이 quota를 소진시킬 수 있음 | 보안·비용 감각 없음 |

---

## 7. 명시적 배제 목록

**이 항목들은 도입하지 않는다. 각 배제 사유는 ADR로 문서화하며, 배제 사유 자체가 포트폴리오 자산이다.**

| 배제 대상 | 사유 |
|---|---|
| **VectorStore / RAG 어드바이저 / 벡터DB** | 본 프로젝트의 KB 검색은 결정론적 역색인 3단계 Fallback + 규칙 기반 동명이인 해소다. 임베딩 유사도로 대체하면 **애초에 제거하려던 모호성(인명 경계, 동음이의)을 다시 도입**한다. "AI 프로젝트니까 RAG"가 가장 흔한 실수이며, **의도적으로 배제하고 이유를 설명하는 것이 도입보다 강한 시그널이다.** |
| **Tool Calling / Function Calling** | "모호 케이스는 LLM에 전달"을 function calling으로 뺄 수 있으나, 현재처럼 후보를 프롬프트에 모두 주입하는 편이 더 저렴하고 예측 가능하다 (LLM 왕복 1회 vs 2~3회). |
| **ChatMemory** | 번역은 stateless 단발 요청. 대화 이력이 없다. |
| **추가 모델 파인튜닝 / 재학습** | 연구 영역으로의 회귀. 서버 직무 평가에 반영되지 않는다. |
| **Kafka** | 이 워크로드 규모(수만~수십만 메시지)에 정당성 없음. Redis Streams Consumer Group으로 충분. 면접에서 "왜요?" 한 번에 무너진다. |
| **마이크로서비스 세분화** | API / Worker / NER 3개 프로세스가 상한. 그 이상 쪼갤 근거가 없다. |
| **이력서용 Kubernetes** | 목표 직무가 백엔드면 과잉. SRE/인프라 타겟일 경우에만 재검토 (§13 참조). |
| **프론트엔드 완성도** | 데모 UI는 기능만. Grafana 대시보드 하나가 예쁜 프론트보다 강하다. |
| **전체 코퍼스 실제 번역** | 유계 구간(인조 1년치)만 실행 후 외삽. 비용과 시간이 감당 불가. |

---

## 8. API 계약 및 데이터 모델

### 8.1 API

```
POST   /api/v1/translations              단건 요청 → 202 { jobId, estimatedCost }
GET    /api/v1/translations/{jobId}      결과 조회
GET    /api/v1/translations/{jobId}/stream   SSE 스트리밍
POST   /api/v1/batches                   배치 생성 (budget_limit 필수) → 202
GET    /api/v1/batches/{batchId}         진행률 / 소진 예산 / 체크포인트
POST   /api/v1/batches/{batchId}/pause   일시정지
POST   /api/v1/batches/{batchId}/resume  체크포인트부터 재개
GET    /actuator/prometheus              메트릭
```

**응답 스키마 (Structured Output 대상)**

```json
{
  "jobId": "...",
  "translatedText": "...",
  "entities": [
    { "surface": "李馨長", "type": "PER", "kbId": "...",
      "resolvedName": "이형장", "confidence": 0.94,
      "fallbackStage": 1, "ambiguous": false }
  ],
  "uncertainSpans": [{ "start": 12, "end": 18, "reason": "KB_MISS" }],
  "meta": {
    "tier": "T1", "model": "gemini-2.5-flash",
    "cacheHit": "L2_TEMPLATE",
    "tokensIn": 412, "tokensOut": 158,
    "costKrw": 1.83, "kbVersion": "injo-v1"
  }
}
```

### 8.2 데이터 모델 (PostgreSQL)

```
translation_job
  id, idempotency_key(uniq), source_text, normalized_hash, template_hash,
  status, tier, model_used, cache_hit_level,
  tokens_in, tokens_out, cost_krw, kb_version,
  batch_id(nullable), created_at, completed_at, error_class

translation_result
  job_id(FK), translated_text, entities(jsonb), uncertain_spans(jsonb)

batch_job
  id, range_spec, budget_limit_krw, spent_krw,
  cursor_checkpoint, total_count, done_count, failed_count,
  status, created_at, updated_at

cost_ledger
  id, job_id, model, tokens_in, tokens_out, unit_price, cost_krw, recorded_at
```

### 8.3 Redis 키 설계

```
cache:l1:{normalized_hash}          → 번역 결과 (원문 완전 일치)
cache:l2:{kb_version}:{template_hash} → 번역 템플릿 (슬롯 치환형)
job:status:{jobId}                  → 상태
rate:bucket:{provider}              → 적응형 토큰 버킷 상태
budget:daily:{yyyy-mm-dd}           → 일일 소진액
stream:translation                  → 작업 큐 (Consumer Group)
stream:translation:dlq              → DLQ
```

---

## 9. 관측성 및 측정 지표

### 9.1 필수 메트릭

| 메트릭 | 타입 | 용도 |
|---|---|---|
| `translation.latency` (tier별, 단계별) | histogram | 지연 예산 검증 (§2.1) |
| `translation.cost.krw` | counter | 비용 SLI |
| `translation.cache.hit` (L1/L2 라벨) | counter | 캐싱 효과 증명 |
| `translation.tier.distribution` | counter | 라우팅 효과 증명 |
| `llm.tokens` (in/out) | counter | Spring AI Micrometer 연동 |
| `llm.rate_limit.429` | counter | 적응형 rate control 입력 |
| `budget.consumption.ratio` | gauge | 예산 서킷브레이커 트리거 |
| `batch.throughput` (문장/시간) | gauge | 완역 시간 모델 입력 |

### 9.2 헤드라인 성공 기준

README 최상단에 다음 형식의 문장이 실측 기반으로 들어가야 한다.

```
· 인조 1년치 N문장 실측 기준, 잔여 1억 5천만 자 완역 추정:
  총 비용 X원 / 소요 Y일 (동시성 Z, 가정 명시)
· 템플릿 슬롯 캐싱으로 LLM 호출 A% 감소, 비용 B% 절감
· NER 기반 티어 라우팅으로 골든셋 품질 동등 유지하며 비용 C% 추가 절감
· 시스템 최대 처리량 D rps, 병목은 (외부 API 동시성 / 워커 수 / ...)
```

---

## 10. 마일스톤

> 각 마일스톤은 수용 기준을 모두 만족해야 완료. **시간이 부족하면 M4·M5를 줄이지 말고 M6의 인프라 범위를 줄인다.** 3개를 깊게 하는 것이 7개를 얕게 하는 것보다 강하다.

### M0 — 리프레이밍 + 비용/시간 모델 (2~3일)

- 잔여 글자 수 → 토큰 환산 → 모델 단가 → 동시성 상한 → 총비용·벽시계 시간 산출
- **모든 가정을 명시적으로 문서화** (토큰/글자 비율, 프롬프트 오버헤드, 재시도율, 실패율)
- `docs/cost-model.md` 작성, 스프레드시트 또는 계산 스크립트 포함
- 선행 연구 자산 이관 (모델 가중치, KB JSON, 골든셋)

**수용 기준:** 가정을 바꾸면 결과가 재계산되는 파라미터화된 모델이 존재한다. 하드코딩된 숫자 금지.

### M1 — 단건 동기 경로 (1주)

- Spring Boot API + Spring AI(google-genai) + Gemini 호출
- Python NER 서버 (ONNX INT8 변환 포함)
- KB in-memory 로더 + 3단계 Fallback 링킹 이식
- PromptTemplate + Structured Output 적용
- **단계별 지연 실측** → §2.1 표를 실측치로 갱신

**수용 기준:** 원문 1건 → 구조화된 번역 결과 반환. §2.1 지연 분해가 실측으로 채워지고 "외부 API가 p95의 N%"가 확정된다. ONNX 변환 전후 NER 지연·정확도 비교표 존재.

### M2 — 비동기 + 배치 엔진 (2주) ★ 가장 중요

- Redis Streams 큐 + Consumer Group + DLQ
- 적응형 rate control (429 피드백 기반)
- 체크포인트 / 일시정지 / 재개
- 멱등 키 (중복 과금 방지)
- 실패 분류별 처리 경로
- Resilience4j 서킷브레이커 + backoff with jitter
- SSE 스트리밍 (단건)

**수용 기준:** 배치 실행 중 강제 종료 → 재시작 시 중복 호출 0건으로 이어서 완료. 429 유발 시 rate가 자동 하향되고 회복 후 상향된다. DLQ에 실패 사유가 분류되어 적재된다.

### M3 — 템플릿 슬롯 캐싱 (1주)

- L1/L2 2단 캐시
- 슬롯 치환 + 재주입 로직
- KB 버저닝 기반 무효화 (ADR-009 결정 따름)
- 재주입 결과 검증 (샘플링 대조)

**수용 기준:** 인조 1년치 실측 L1/L2 히트율 수치 확보. 캐시 on/off 비용 비교표. 재주입 실패 케이스가 탐지되어 fallback(전체 파이프라인 재실행)된다.

### M4 — NER 비용 라우팅 (1주)

- 티어 판정기 구현
- 티어별 모델 매핑 (ChatClient provider 교체 활용)
- 골든셋 기반 품질 검증

**수용 기준:** 라우팅 on/off의 비용/품질 곡선. 품질 저하 임계 이내에서 비용 절감률 수치 확보.

### M5 — 비용 SLI + 예산 서킷브레이커 (3~4일)

- Grafana 대시보드 (§9.1 전 항목)
- 3단계 degradation
- 배치 예산 사전 검증

**수용 기준:** 예산 임계 도달을 인위적으로 유발했을 때 3단계 저하가 순서대로 동작한다. 대시보드 스크린샷이 README에 포함된다.

### M6 — 배포 / CI / 부하 테스트 / 문서화 (1주)

- GitHub Actions: 테스트 → 이미지 빌드 → GHCR 푸시 → 배포
- 비결정 출력 CI (골든셋 + 통계 임계값)
- k6 부하 테스트 (외부 API mock) → 내부 처리량 한계 및 병목 식별
- README 재작성 (헤드라인 숫자 최상단)
- ADR 전체 정리

**수용 기준:** "내 시스템의 최대 처리량은 D rps이며 병목은 X"를 실측 근거와 함께 말할 수 있다. ADR 13개 이상 존재.

---

## 11. ADR 목록

`docs/adr/NNN-title.md` 형식. 각 문서는 **배경 / 선택지 / 결정 / 버린 대안과 그 이유 / 재검토 조건**을 포함한다.
**버린 대안이 핵심이다.** 이 섹션이 없는 ADR은 미완성으로 간주한다.

| # | 제목 |
|---|---|
| 001 | 비동기 큐 기반 처리 채택 (동기 요청/응답 배제) |
| 002 | Python NER 서버 프로세스 분리 |
| 003 | GPU 배제 및 ONNX INT8 CPU 서빙 채택 |
| 004 | KB in-memory 유지, Redis 외부화 배제 |
| 005 | Spring AI 채택 및 버전(2.0.0 GA) 선택 |
| 006 | 벡터DB / RAG 어드바이저 배제 |
| 007 | Tool Calling 배제 |
| 008 | ChatMemory 배제 |
| 009 | 템플릿 슬롯 캐싱 및 KB 버저닝 무효화 전략 |
| 010 | NER 기반 티어 라우팅 도입 |
| 011 | 예산 기반 서킷브레이커 및 단계적 저하 |
| 012 | Kafka / 마이크로서비스 세분화 / K8s 배제 |
| 013 | 비결정 LLM 출력에 대한 CI 회귀 검증 전략 |
| 014 | Redis Streams vs RabbitMQ 큐 선택 |
| 015 | 엔티티 링킹 정본 결정 (README 기술 vs 실제 코드 불일치) |

---

## 12. 리포지토리 구조

```
.
├── README.md                  # 헤드라인 숫자 최상단
├── PROJECT_PLAN.md            # 본 문서
├── docs/
│   ├── adr/                   # 001~015
│   ├── cost-model.md          # M0 산출물
│   ├── architecture.md
│   ├── benchmarks.md          # 지연·처리량·캐시 히트율 실측
│   └── troubleshooting.md     # Spring AI 함정 등
├── api/                       # Spring Boot — API 서버
├── worker/                    # Spring Boot — 파이프라인 워커
├── ner-server/                # Python + FastAPI + ONNX
├── kb/                        # 선행 연구 KB JSON + 로더
├── eval/                      # 골든셋 + 회귀 검증
├── load-test/                 # k6 스크립트
├── deploy/                    # Docker Compose / CI 설정
├── grafana/                   # 대시보드 JSON
└── research/                  # 선행 연구 코드·문서 보존 (01_크롤러 ~ 06_NER)
```

---

## 13. 리스크 및 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| **공개 배포 시 타인의 quota 소진** | 비용 폭증 | API 키 발급 + 사용자별 rate limit + 일일 예산 상한. **설계 초기부터 포함** (그 자체가 포폴 소재) |
| Spring AI 2.0 생태계 미성숙 | 개발 지연 | 블로커 발생 시 1.1.x 폴백. ADR-005에 기록 |
| 템플릿 재주입 품질 저하 | 캐싱 축 무력화 | 샘플링 검증 + 실패 시 전체 파이프라인 fallback. 히트율이 낮으면 L1만 유지하고 축을 축소 |
| 티어 라우팅의 품질 손실 | 차별화 축 실패 | 골든셋 임계 미달 시 해당 티어 비활성화. **"시도했고 이 조건에서 안 됐다"도 유효한 결과** |
| 범위 확대로 미완성 | 전부 얕아짐 | M0~M3까지가 최소 완성선. M4·M5는 시간이 남을 때만 |
| **목표 직무 불일치** | 노력 방향 오류 | **결정 완료 (2026-08-31): 백엔드 개발 타겟. 현 계획 그대로 진행.** |

---

## 14. 최종 산출물 체크리스트

- [ ] README 최상단에 실측 기반 헤드라인 숫자 4개 (§9.2)
- [ ] `docs/cost-model.md` — 파라미터화된 완역 비용/시간 모델
- [ ] `docs/benchmarks.md` — 지연 분해, 캐시 히트율, 티어 분포, 처리량 한계
- [ ] ADR 15개 (각각 "버린 대안과 이유" 포함)
- [ ] Grafana 비용 대시보드 스크린샷
- [ ] 배치 중단→재개 데모 (중복 호출 0건 증명)
- [ ] 예산 서킷브레이커 3단계 저하 동작 데모
- [ ] k6 부하 테스트 리포트 + 병목 식별
- [ ] CI 파이프라인 (테스트 → 빌드 → 배포) 동작
- [ ] 골든셋 회귀 검증 CI 동작

---

## 부록 A — 면접 대비 예상 질문

각 질문에 대한 답이 ADR에 존재해야 한다.

1. 왜 동기 API가 아니라 큐를 썼는가?
2. NER 추론에 GPU를 안 쓴 이유는? (→ 측정 결과 제시)
3. AI 프로젝트인데 왜 벡터DB / RAG를 안 썼는가? (→ **가장 중요한 질문**)
4. 왜 Kafka가 아니라 Redis Streams인가?
5. 요청 1건당 비용이 얼마인가? 어떻게 아는가?
6. 배치가 중간에 죽으면 어떻게 되는가? 중복 과금은?
7. 429가 몰아치면 시스템이 어떻게 반응하는가?
8. Spring AI를 안 쓰고 WebClient로 직접 호출했다면 뭐가 달라졌는가?
9. 이 시스템의 최대 처리량과 병목은?
10. LLM 응답이 비결정적인데 회귀 테스트를 어떻게 하는가?
11. KB가 갱신되면 캐시는 어떻게 되는가?
12. 이 아키텍처에서 가장 후회하는 결정은? (→ 정직하게 답할 것이 준비되어 있어야 함)

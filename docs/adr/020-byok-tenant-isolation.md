# ADR-020: BYOK / 테넌트 격리 채택, 키 비저장 원칙

- 상태: 승인 (M2.5-S6에서 구현)
- 날짜: 2026-09-01

## 배경

무료 quota는 RPD 20(T1 실측)이다. 본인 키로 공개하면 한 사람이 오전에 소진한다 (D10).
"연구자가 자기 사료에 쓸 수 있는 형태"(§1.3)가 목표인 이상 공개는 전제이고, 그렇다면
quota는 요청자의 것이어야 한다. 그 순간 시스템의 문제는 "테넌트별 rate·예산 격리"로 바뀐다.

## 선택지

1. **BYOK (X-Llm-Key) + 발급형 테넌트 키 (X-Api-Key) + 테넌트×모델 격리**
2. 운영자 키 단일 + 강한 전역 rate limit
3. 테넌트 LLM 키를 암호화 저장하고 재사용

## 결정

**선택지 1.** 두 키는 축이 다르다:

- **X-Api-Key (발급형)** — 테넌트 식별. DB에는 SHA-256 해시만 저장. 없으면 default 테넌트(개발·단독 운영),
  미등록이면 401. 일일 상한은 `budget:daily:{tenant}:{date}`(Redis INCR)로 수용 시점에 선과금 — 초과 시 429,
  LLM 호출 없이 거부된다. 배치는 job 수만큼 선과금.
- **X-Llm-Key (BYOK)** — 요청자의 LLM 키. **어떤 테이블·로그·에러 응답에도 저장하지 않는다.**
  `TranslatorFactory.forModelWithKey()`가 요청 단위 클라이언트를 만들고 요청과 함께 버린다.
  검증: 엉터리 키 → provider "API key not valid"(공급 키로 실제 호출됨), 로그에 키 원문 0회.
- **격리 단위 = 테넌트×모델**: rate 버킷 `rate:bucket:{tenant}:{model}` (BYOK에서는 quota 풀 자체가
  테넌트별이므로), 원장·job·배치에 `tenant_id`, 메트릭 `tenant.calls`.

## 한계 (정직하게)

- **BYOK는 동기/SSE 경로만이다.** 비동기 배치는 키가 api→worker로 건너가야 하는데, Redis Stream에 실으면
  "저장하지 않는다"가 깨진다 (스트림은 영속이다). 배치 BYOK는 안전한 키 전달 설계(세션 스코프 등)와 함께
  별도 단계로 미룬다 — 지금 배치는 운영자 키(default 테넌트) 전용.
- default 테넌트는 공개 배포 시 비활성화해야 한다 (M6 체크리스트).

## 버린 대안과 그 이유

- **운영자 키 단일(2)**: rate limit을 아무리 잘 깎아도 RPD 20은 20이다. 나눠 쓰는 구조 자체가 성립 불가.
- **키 암호화 저장(3)**: 키 관리(회전·폐기·유출 대응)라는 별도 시스템을 떠안는다. 비저장이면 유출할 것이
  없다 — 지금 필요한 것은 재사용이 아니라 요청 처리이므로 저장할 이유가 없다. 저장이 필요해지는 순간
  (배치 BYOK) 별도 ADR로 결정한다.

## 재검토 조건

- 배치 BYOK 요구가 실제로 생기면: 키를 잡 수명 동안만 보관하는 세션 스코프 저장(암호화+TTL)을 설계.
- 테넌트가 늘어 발급·회전 관리가 필요해지면 관리 API(키 재발급·폐기)를 추가.

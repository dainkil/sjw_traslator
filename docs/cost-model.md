# 잔여 코퍼스 완역 비용/시간 모델 (M0)

계산 스크립트: [`cost-model/cost_model.py`](cost-model/cost_model.py). 모든 수치는 파라미터이며, 가정을 바꾸면 결과가 재계산된다.

```
python3 docs/cost-model/cost_model.py                            # 기준 시나리오
python3 docs/cost-model/cost_model.py --cache-hit-rate 0.4 --batch-discount
python3 docs/cost-model/cost_model.py --model flash-lite
```

## 파라미터와 출처

| 파라미터 | 기본값 | 출처 |
|---|---|---|
| 총 글자 수 | 242,500,000 | 선행 연구 README |
| 국역 완료율 | 37.4% | 선행 연구 README |
| 원문 평균 글자/문장 | 126.1 | **[실측]** `Merged_Corpus_Final.json` 62,476쌍 (2026-08-31) |
| 번역/원문 길이비 | 2.114 | **[실측]** 동일 |
| 한자 토큰/자 | 0.954 | **[실측]** `countTokens`, 골든셋 300문장 전수 (2026-08-31, `measure_tokens.py`) |
| 한국어 토큰/자 | 0.630 | **[실측]** 동일 |
| 프롬프트 오버헤드 | 448 tok | **[실측]** 페르소나 고정부. KB 블록 주입분(문장별 가변, +50~150 추정)은 미포함 |
| 재시도 오버헤드 | 5% | **(추정)** M2 실측 교체 |
| 캐시 히트율 | 0% | M3 전 기준. M3 실측 교체 |
| Flash 단가 | $0.30 / $2.50 per 1M tok | **[공시가]** 2026-08-31 확인. 배치 API 50% 할인 별도 |
| Flash-Lite 단가 | $0.10 / $0.40 | **[공시가]** 동일. 단, 2.5-flash-lite는 **단종 확인(2026-08-31 API 404)** — 저가 티어 후속은 3.x lite 계열로 재조사 필요 |
| 무료 티어 RPD | 20 (gemini-3.5-flash) | **[실측]** 429 응답의 `quotaValue` (2026-08-31). 모델별 독립 quota — 타 모델 RPD는 M2에서 소진 실측 |
| 프롬프트 오버헤드 (E2E) | 700 tok | **[실측 역산]** usage 입력 ~820 - 원문 ~120 |
| LLM 호출 지연 | 3.2s (flash-lite) / 12.1s (3.5-flash) | **[실측]** `eval/measure_e2e.py`, docs/benchmarks.md |
| 환율 | 1,400 KRW/USD | **(추정)** |
| LLM 호출 지연 | 2.0s | **(추정)** M1 실측 교체 |
| 동시성 | 50 | 설계 변수 |

## 기준 시나리오 산출 (토큰 실측 반영, 2026-08-31 — 지연·재시도율·환율은 아직 추정)

| 시나리오 | 총 비용 | 문장당 | 벽시계 |
|---|---|---|---|
| Flash 단가 기준, 캐시 0% | **$842 ≈ 118만 원** | 1.0원 | 0.9일 @동시성50 |
| 캐시 40% + 배치 할인 | $253 ≈ 35만 원 | 0.3원 | 0.5일 |
| Flash-Lite 단가 기준, 캐시 0% | $177 ≈ 25만 원 | 0.2원 | 0.9일 |
| **무료 티어 (운영 기본, ADR-016)** | **0원** | 0원 | **RPD 20 (3.5-flash 단독, 실측) 기준 173년** / 멀티모델 풀링·고RPD 모델 실측 시 단축 (M2) |

> **이 대비가 프로젝트의 논지다**: 동일 시스템이 무료 quota(단일 모델 실측)로는 173년, 118만 원이면 하루 안에 끝난다.
> 운영은 무료 티어로 하되(quota=예산 원장), 유료 시나리오는 파라미터 하나로 재계산되는 counterfactual로 유지한다.
> 무료 축의 단축 수단이 곧 이 시스템의 기능 목록이다: 티어 라우팅=모델별 quota 풀링(M4), 템플릿 캐싱=호출 자체 제거(M3).

> 최초 추정(전부 추정 파라미터) 대비 총비용이 $1,191 → $746로 내려갔다. 주 원인은 프롬프트 고정부가
> 추정 1,200tok → 실측 448tok(Gemini 토크나이저가 한국어 페르소나를 예상보다 압축), 한자 토큰/자 1.3 → 0.954.

## 모델이 드러낸 구조적 사실 2가지

1. **입력 토큰의 ~85%가 프롬프트 오버헤드다** (E2E usage 실측: 호출당 입력 ~820 tok = 페르소나 448 + Structured Output 지시문·KB블록 ~250 + 원문 ~120).
   따라서 비용 절감의 1순위 레버는 번역 결과 캐싱 이전에 **프롬프트 다이어트와 컨텍스트 캐싱**(Gemini cached input $0.03/M — 일반 입력의 1/10)이다. → M3에서 고정부 context cache 분리 실험을 함께 다룬다.
2. **벽시계 시간은 지연이 아니라 quota가 지배할 것이다.** 동시성 50 × 2s면 이론상 25 rps지만, 실제 상한은 Gemini의 RPM/TPM quota다. 이 모델은 quota를 반영하지 않으므로 벽시계 수치는 하한값이다. M2의 적응형 rate control 실측으로 유효 처리량을 확정한다.

## 리스크

- **Gemini 2.5 계열 은퇴**: 공식 deprecations 페이지 확인 결과(2026-08-31) `gemini-2.5-flash`는 **은퇴일 미공지** — 웹에 돌던 2026-10-16설은 공식 아님. 기본 티어 유지. 단, Gemini 3.x 계열(3.5/3.6/3.7-flash)이 활성 라인이므로 은퇴 공지 시 ChatClient 추상화(ADR-005)로 교체하고 단가 파라미터만 갱신한다. 3.x는 M4 티어 라우팅의 모델 후보이기도 하다.
- 문장 분할 단위가 선행 코퍼스와 다르면 문장 수 산정이 달라진다. `avg_src_chars_per_sentence`가 이를 흡수한다.

## 실측 교체 일정

| 파라미터 | 교체 시점 | 방법 |
|---|---|---|
| 토큰/자 비율, 프롬프트 오버헤드 | ~~M1~~ **완료 (2026-08-31)** | `countTokens` 300문장 + E2E usage 역산 |
| 호출 지연 | ~~M1~~ **완료 (2026-08-31)** | 단계별 지연 분해 — LLM이 p95의 99.7~99.9% 확정 |
| 모델별 무료 RPD | M2 | 배치 실행 중 소진 시점의 `quotaValue` 수집 (3.5-flash=20 확정) |
| 재시도율, 유효 처리량 | M2 | 인조 1년치 배치 실행 |
| 캐시 히트율 | M3 | L1/L2 on/off 비교 |
